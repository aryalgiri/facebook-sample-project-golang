package config

const (
	//DBHost is the hostname of postgres DB
	DBHost = "postgresql.cf3itk3bbcdw.us-east-2.rds.amazonaws.com"
	//DBUserName is the user name of the postgesql DV
	DBUserName = "postgres"
	//DBPassword is the password for the postgresql
	DBPassword = "Test1234"
	// DBName is the name of the database
	DBName = "postgres"
	//DBPort is the port for postgres
	DBPort = 5432
	// Port is where our service is running
	Port = "8080"
)

package config

const (
	// DBHost is the hostname of postgres DB
	DBHost = ""
	// DBUserName is the user name of the postgresql DB
	DBUserName = ""
	// DBPassword is the password of the postgresql DB
	DBPassword = ""
	// DBName is the name of the database
	DBName = "postgres"
	// DBPort is the port for postgresql
	DBPort = 5432
	// Port is where our service is running
	Port = "8080"
)
